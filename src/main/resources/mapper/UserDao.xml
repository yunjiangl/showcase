<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.exqoo.dao.UserDao">

	<select id="selectUserByUserName" resultType="com.exqoo.entity.UserEntity">
		SELECT * FROM nfdw_users WHERE user_name = #{username}
	</select>
	
	<insert id="add">
		INSERT INTO nfdw_users(user_name, user_password,user_createdate) VALUES (#{uname},#{upassword},#{createdate})
	</insert>


	<update id="updatePassword" parameterType="map">
		update nfdw_users set `user_password` = #{newPassword} 
			where user_id = #{userId} and user_password = #{password}
	</update>
</mapper>